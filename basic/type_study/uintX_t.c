#include <stdio.h>
#include <stdint.h>

int main() {
	printf("uint8_t  size = %lu\n", sizeof(uint8_t));
	printf("uint16_t size = %lu\n", sizeof(uint16_t));
	printf("uint32_t size = %lu\n", sizeof(uint32_t));
	printf("uint64_t size = %lu\n", sizeof(uint64_t));
	return 0;
}
