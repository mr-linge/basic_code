
aarch64-linux-gnu-gcc -fPIC -shared test.c -o libtest.so

# -l  选项告诉编译器要使用test这个库
# -L. 告诉编译器在当前目录中查找库文件
aarch64-linux-gnu-gcc -no-pie main.c -o tracee -L. -ltest -ldl

#  把当前路径加入环境变量LD_LIBRARY_PATH中
export LD_LIBRARY_PATH=.:$LD_LIBRARY_PATH

./tracee
